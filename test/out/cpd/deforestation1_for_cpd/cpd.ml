open GT
open OCanren
open OCanren.Std
open Helper

let topLevel x0 = 
  let rec rr y0 = (rR y0) 
  and rR y1 = (r y1) 
  and r y3 = (y3 === (((Nat.succ Nat.zero)) % ((List.nil ())))) 
  in    (rr x0)
